<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KUPPAYAM Admin Login</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #333333;
            --accent: #e63946;
            --text: #333333;
            --text-secondary: #666666;
            --background: #ffffff;
            --border: #e1e1e1;
            --hover: #f8f9fa;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            min-height: 100vh;
            display: flex;
            background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
            position: relative;
            color: var(--text);
        }

        .background-pattern {
            position: fixed;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(90deg, #f8f9fa 2px, transparent 2px) 0 0,
                linear-gradient(180deg, #f8f9fa 2px, transparent 2px) 0 0;
            background-size: 50px 50px;
            opacity: 0.4;
        }

        .login-container {
            width: 100%;
            max-width: 1000px;
            margin: auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            background: var(--background);
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            position: relative;
            z-index: 1;
        }

        .brand-side {
            background: linear-gradient(145deg, #ffffff, #f8f9fa);
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            border-right: 1px solid var(--border);
        }

        .logo {
            font-size: 2.5em;
            color: var(--primary);
            margin-bottom: 20px;
            font-weight: bold;
            letter-spacing: 2px;
        }

        .brand-tagline {
            color: var(--text-secondary);
            text-align: center;
            font-size: 1.1em;
            margin-top: 20px;
        }

        .login-side {
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background: var(--background);
        }

        .login-header {
            margin-bottom: 40px;
            text-align: center;
        }

        .login-header h1 {
            color: var(--text);
            font-size: 2em;
            margin-bottom: 10px;
        }

        .login-header p {
            color: var(--text-secondary);
        }

        .form-group {
            margin-bottom: 25px;
            position: relative;
        }

        .form-group input {
            width: 100%;
            padding: 15px 20px;
            border: 1px solid var(--border);
            border-radius: 8px;
            font-size: 1em;
            transition: all 0.3s ease;
            background: var(--background);
            color: var(--text);
        }

        .form-group input:focus {
            border-color: var(--accent);
            outline: none;
            box-shadow: 0 0 0 3px rgba(230, 57, 70, 0.1);
        }

        .form-group label {
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-secondary);
            pointer-events: none;
            transition: all 0.3s ease;
        }

        .form-group input:focus + label,
        .form-group input:not(:placeholder-shown) + label {
            top: 0;
            left: 15px;
            font-size: 0.8em;
            padding: 0 5px;
            background: var(--background);
            color: var(--accent);
        }

        .login-button {
            background: var(--accent);
            color: white;
            border: none;
            padding: 15px;
            border-radius: 8px;
            font-size: 1.1em;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .login-button:hover {
            background: #d63140;
            transform: translateY(-1px);
        }

        @media (max-width: 768px) {
            .login-container {
                grid-template-columns: 1fr;
                max-width: 400px;
                margin: 20px;
            }

            .brand-side {
                padding: 30px;
                border-right: none;
                border-bottom: 1px solid var(--border);
            }
        }

        .fashion-icon {
            color: var(--text-secondary);
            margin: 10px;
            font-size: 1.2em;
            transition: all 0.3s ease;
        }

        .fashion-icon:hover {
            color: var(--accent);
        }

        .brand-icons {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .decorative-circles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            pointer-events: none;
            opacity: 0.1;
        }

        .circle {
            position: absolute;
            background: var(--primary);
            border-radius: 50%;
        }

        .circle-1 {
            width: 100px;
            height: 100px;
            top: -50px;
            left: -50px;
        }

        .circle-2 {
            width: 150px;
            height: 150px;
            bottom: -75px;
            right: -75px;
        }
    </style>
</head>
<body>
    <div class="background-pattern"></div>
    <div class="login-container">
        <div class="brand-side">
            <div class="decorative-circles">
                <div class="circle circle-1"></div>
                <div class="circle circle-2"></div>
            </div>
            <div class="logo">KUPPAYAM</div>
            <p class="brand-tagline">Fashion Forward Admin Portal</p>
            <div class="brand-icons">
                <i class="fas fa-tshirt fashion-icon"></i>
                <i class="fas fa-hat-cowboy fashion-icon"></i>
                <i class="fas fa-socks fashion-icon"></i>
            </div>
        </div>
        <div class="login-side">
            <div class="login-header">
                <h1>Welcome Back</h1>
                <p>Please sign in to continue</p>
            </div>
            <form action="/admin/login" method="POST">
                <div class="form-group">
                    <input type="text" id="username" name="email" required placeholder=" ">
                    <label for="username">Username or Email</label>
                </div>
                <div class="form-group">
                    <input type="password" id="password" name="password" required placeholder=" ">
                    <label for="password">Password</label>
                </div>
                <button type="submit" class="login-button">
                    Login to Dashboard
                </button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if (message) { %>
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                Swal.fire({
                    text: '<%= message %>',
                    icon: 'error',
                    toast: true,
                    position: 'top-end',
                    showConfirmButton: false,
                    timer: 3000,
                    timerProgressBar: true,
                    background: '#ffffff',
                    color: '#333333'
                });
            });
        </script>
    <% } %>
</body>
</html>