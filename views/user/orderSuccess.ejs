<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <style>
        @keyframes floatIn {
            0% { transform: translateY(-70px); opacity: 0; }
            100% { transform: translateY(0); opacity: 1; }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes checkmark {
            0% { stroke-dashoffset: 100; opacity: 0; }
            100% { stroke-dashoffset: 0; opacity: 1; }
        }

        @keyframes confetti {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
        }

        @keyframes shine {
            0% { background-position: -100px; }
            100% { background-position: 200px; }
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #f8f9fa 0%, #e2e6ea 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            color: #2c3e50;
        }

        .success-container {
            max-width: 650px;
            margin: 40px auto;
            padding: 50px 40px;
            text-align: center;
            background-color: #ffffff;
            box-shadow: 0 15px 35px rgba(50, 50, 93, 0.1), 0 5px 15px rgba(0, 0, 0, 0.07);
            border-radius: 24px;
            animation: floatIn 0.8s cubic-bezier(0.22, 0.61, 0.36, 1);
            position: relative;
            overflow: hidden;
        }

        .success-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(90deg, #4CAF50, #45b649, #8BC34A);
        }

        .confetti {
            position: absolute;
            width: 10px;
            height: 10px;
            opacity: 0;
        }

        .confetti-1 { top: -10px; left: 10%; background: #FFC107; animation: confetti 3s ease-out 0.5s forwards; }
        .confetti-2 { top: -10px; left: 20%; background: #FF5722; animation: confetti 2.5s ease-out 0.7s forwards; }
        .confetti-3 { top: -10px; left: 30%; background: #E91E63; animation: confetti 2.7s ease-out 0.9s forwards; }
        .confetti-4 { top: -10px; left: 40%; background: #9C27B0; animation: confetti 3.2s ease-out 0.3s forwards; }
        .confetti-5 { top: -10px; left: 50%; background: #2196F3; animation: confetti 2.9s ease-out 0.6s forwards; }
        .confetti-6 { top: -10px; left: 60%; background: #00BCD4; animation: confetti 3.1s ease-out 0.8s forwards; }
        .confetti-7 { top: -10px; left: 70%; background: #8BC34A; animation: confetti 2.8s ease-out 0.5s forwards; }
        .confetti-8 { top: -10px; left: 80%; background: #CDDC39; animation: confetti 2.6s ease-out 0.4s forwards; }
        .confetti-9 { top: -10px; left: 90%; background: #F44336; animation: confetti 3s ease-out 0.7s forwards; }

        .success-icon {
            width: 100px;
            height: 100px;
            margin: 0 auto 30px;
            position: relative;
        }

        .checkmark-circle {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background-color: #f0f8f0;
            display: flex;
            align-items: center;
            justify-content: center;
            animation: fadeIn 0.5s ease-in-out 0.3s both;
        }

        .checkmark-svg {
            width: 60px;
            height: 60px;
        }

        .checkmark-path {
            fill: none;
            stroke: #4CAF50;
            stroke-width: 5;
            stroke-linecap: round;
            stroke-dasharray: 100;
            stroke-dashoffset: 100;
            animation: checkmark 1s ease-in-out 0.8s forwards;
        }

        .success-message h1 {
            color: #2c3e50;
            margin: 20px 0;
            font-size: 36px;
            font-weight: 700;
            animation: fadeIn 0.8s ease-out 1s both;
            position: relative;
            display: inline-block;
        }

        .success-message h1::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            width: 60%;
            height: 3px;
            background: linear-gradient(90deg, transparent, #4CAF50, transparent);
        }

        .success-message p {
            color: #606c7c;
            font-size: 18px;
            margin-bottom: 30px;
            line-height: 1.7;
            animation: fadeIn 0.8s ease-out 1.2s both;
        }

        .order-details {
            background-color: #f8f9fa;
            padding: 25px;
            border-radius: 16px;
            margin: 30px 0;
            animation: fadeIn 0.8s ease-out 1.4s both;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            border-left: 4px solid #4CAF50;
        }

        .order-details h3 {
            margin-top: 0;
            color: #3e4b5b;
            font-size: 20px;
        }

        .order-number {
            font-family: monospace;
            background: #eef2f7;
            padding: 8px 15px;
            border-radius: 50px;
            font-weight: bold;
            color: #3a4452;
            display: inline-block;
            margin: 10px 0;
            letter-spacing: 1px;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding-bottom: 10px;
            border-bottom: 1px dashed #e0e6ec;
        }

        .detail-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }

        .detail-label {
            font-weight: 600;
            color: #5a6a7e;
        }

        .detail-value {
            color: #2c3e50;
        }

        .button-container {
            display: flex;
            justify-content: center;
            gap: 25px;
            margin-top: 35px;
            animation: fadeIn 0.8s ease-out 1.6s both;
        }

        .btn {
            padding: 15px 35px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            animation: shine 2s infinite linear;
        }

        .btn-icon {
            font-size: 20px;
        }

        .continue {
            background: linear-gradient(135deg, #4CAF50, #45b649);
            color: white;
            box-shadow: 0 6px 15px rgba(76, 175, 80, 0.3);
        }

        .orders {
            background: linear-gradient(135deg, #2196F3, #1a7fd1);
            color: white;
            box-shadow: 0 6px 15px rgba(33, 150, 243, 0.3);
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .btn:active {
            transform: translateY(1px);
        }

        .thanks-animation {
            margin-top: 40px;
            font-size: 40px;
            animation: bounce 2s infinite;
            color: #4CAF50;
        }

        .delivery-tracker {
            display: flex;
            justify-content: space-between;
            margin: 30px 0;
            position: relative;
            padding: 0 10px;
        }

        .tracker-line {
            position: absolute;
            top: 15px;
            left: 15%;
            right: 15%;
            height: 4px;
            background-color: #e0e6ec;
            z-index: 1;
        }

        .tracker-progress {
            position: absolute;
            top: 15px;
            left: 15%;
            width: 33%;
            height: 4px;
            background: linear-gradient(90deg, #4CAF50, #8BC34A);
            z-index: 2;
            transition: width 1s ease;
        }

        .tracker-step {
            position: relative;
            z-index: 3;
            text-align: center;
            width: 30px;
        }

        .step-circle {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            background-color: white;
            border: 3px solid #e0e6ec;
            margin: 0 auto 10px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .step-active .step-circle {
            border-color: #4CAF50;
            background-color: #4CAF50;
            color: white;
        }

        .step-label {
            font-size: 12px;
            font-weight: 600;
            color: #95a5a6;
            white-space: nowrap;
        }

        .step-active .step-label {
            color: #4CAF50;
        }

        @media (max-width: 768px) {
            .success-container {
                margin: 20px;
                padding: 30px 20px;
            }

            .button-container {
                flex-direction: column;
                gap: 15px;
            }

            .btn {
                width: 100%;
            }

            .delivery-tracker {
                padding: 0;
            }

            .tracker-line, .tracker-progress {
                left: 10%;
                right: 10%;
            }

            .step-label {
                font-size: 10px;
            }
        }
    </style>
</head>
<body>
    <%- include('../../views/partials/user/header') %>

    <div class="success-container">
        <!-- Confetti animations -->
        <div class="confetti confetti-1"></div>
        <div class="confetti confetti-2"></div>
        <div class="confetti confetti-3"></div>
        <div class="confetti confetti-4"></div>
        <div class="confetti confetti-5"></div>
        <div class="confetti confetti-6"></div>
        <div class="confetti confetti-7"></div>
        <div class="confetti confetti-8"></div>
        <div class="confetti confetti-9"></div>

        <div class="success-icon">
            <div class="checkmark-circle">
                <svg class="checkmark-svg" viewBox="0 0 52 52">
                    <circle class="checkmark-circle" cx="26" cy="26" r="25" fill="none"/>
                    <path class="checkmark-path" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
                </svg>
            </div>
        </div>

        <div class="success-message">
            <h1>Order Confirmed!</h1>
            <p>Thank you for shopping with us. Your order has been successfully placed and is now being prepared for shipment.</p>

            <div class="order-details">
                <h3>Order Summary</h3>
                <div class="order-number">ORD-<%= Math.floor(Math.random() * 900000) + 100000 %></div>
                
                <div class="detail-item">
                    <span class="detail-label">Date:</span>
                    <span class="detail-value"><%= new Date().toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">Estimated Delivery:</span>
                    <span class="detail-value"><%= new Date(Date.now() + 7*24*60*60*1000).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %> - <%= new Date(Date.now() + 10*24*60*60*1000).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %></span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">Status:</span>
                    <span class="detail-value" style="color: #4CAF50; font-weight: bold;">Processing</span>
                </div>
            </div>
            
          
            <p>You will receive a confirmation email shortly with all the details.</p>

            <div class="button-container">
                <a href="/shop" class="btn continue">
                    <span class="btn-icon">🛍️</span>
                    Continue Shopping
                </a>
                <a href="/orderDetails" class="btn orders">
                    <span class="btn-icon">📋</span>
                    View Orders
                </a>
            </div>
            
            
        </div>
    </div>

    <%- include('../../views/partials/user/footer') %>

    <script>
        // Animate progress bar over time to simulate order processing
        setTimeout(() => {
            document.querySelector('.tracker-progress').style.width = '33%';
        }, 1000);
    </script>
</body>
</html>